#include <bits/stdc++.h>
using namespace std;

#define int long long
#define _for(i, n) for(int32_t i = 0; i < (int32_t)n; ++i)
#define r (rand() * ((rand() & 1) ? 1ll : -1ll))

class Solution {
public:
    void test(void) {
        _for(i, 10)
            cout << r << ' ';
    }
};

#ifdef __TEST__
__TEST__
#endif