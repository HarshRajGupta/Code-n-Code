#include <bits/stdc++.h>
using namespace std;

#ifndef debug
#define debug(...)
#endif

#define rep(i, a, n) for(int32_t i = a; i < (int32_t)n; ++i)

class Solution {
public:
    /* function */
    void test() {
        set<int> arr[2];
        arr[0].insert(0);
        debug(arr)
        cout << rank<set<int>[]>::value;
    }
};

#ifdef __TEST__
__TEST__
#endif